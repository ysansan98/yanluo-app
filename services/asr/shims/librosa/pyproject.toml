[build-system]
requires = [
  "setuptools>=70.1",
  "wheel"
]
build-backend = "setuptools.build_meta"

[project]
name = "librosa"
version = "0.10.2"
description = "Minimal librosa shim for qwen-asr"
requires-python = ">=3.9"
dependencies = [
  "numpy",
  "soundfile",
]
