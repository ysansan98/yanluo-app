# Windows 热键验收结果记录（模板）

- 测试日期：
- 测试人：
- 分支：
- 提交：
- 机器型号：
- Windows 版本：
- 键盘布局（输入法/语言）：

## 构建与启动

| 项目 | 结果(PASS/FAIL) | 备注 |
|---|---|---|
| `pnpm run keyboard:build:win` |  |  |
| `pnpm run keyboard:prepare` |  |  |
| `pnpm dev` 启动成功 |  |  |

## 核心触发（P0）

| 用例 | 结果(PASS/FAIL) | 备注 |
|---|---|---|
| 按下并保持快捷键触发会话开始 |  |  |
| 松开快捷键触发会话结束 |  |  |
| 连续触发 10 次稳定 |  |  |

## 修饰键与别名（P0）

| 用例 | 结果(PASS/FAIL) | 备注 |
|---|---|---|
| LeftCtrl + 主键 |  |  |
| RightCtrl + 主键 |  |  |
| LeftAlt + 主键 |  |  |
| RightAlt + 主键 |  |  |
| LeftShift + 主键 |  |  |
| RightShift + 主键 |  |  |
| Win/Meta 组合（如适用） |  |  |

## 快捷键热更新（P0）

| 用例 | 结果(PASS/FAIL) | 备注 |
|---|---|---|
| A -> B 更新后立即生效（无需重启） |  |  |
| 更新后旧快捷键 A 不再触发 |  |  |
| 更新后无粘键 |  |  |

## 预过滤行为（P1）

| 用例 | 结果(PASS/FAIL) | 备注 |
|---|---|---|
| 非目标按键不会触发流程 |  |  |
| 目标组合按键事件可正常驱动匹配 |  |  |
| 切换快捷键后旧组合不影响触发 |  |  |

## 边界场景（P1）

| 用例 | 结果(PASS/FAIL) | 备注 |
|---|---|---|
| 锁屏/解锁后恢复 |  |  |
| 睡眠/唤醒后恢复 |  |  |
| 失焦后仍可全局触发 |  |  |
| 快速连按无异常状态 |  |  |

## 回归检查

| 项目 | 结果(PASS/FAIL) | 备注 |
|---|---|---|
| `pnpm run lint` |  |  |
| `pnpm run typecheck` |  |  |
| `pnpm run test:unit` |  |  |

## 缺陷记录

| 编号 | 严重级别(P0/P1/P2) | 现象 | 复现步骤 | 日志 | 状态 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

## 结论

- 是否通过本轮验收：
- 未通过项汇总：
- 建议处理优先级：
